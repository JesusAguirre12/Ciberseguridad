la primera solucion es empezar con objdump -d archivo

luego vemos todas las lineas y en main hay una en particular en donde indica que flag esta en 2010

luego con objdump -h archivo

podemos ver perfectamente que este cae en algo que se llama rodata y al ver rodata con un 

objdump -j -s .rodata archivo

vemos la flag

otra manera es viendo lo que hay directamene en 2010 con el comando

objdump -s -j .rodata --start-address=0x2010 --stop-address=0x202c sw-05
aca se cual es el tamaño pq chequee que hay en rodata pero es para que se vean start adress y stop adress
