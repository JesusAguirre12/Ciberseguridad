primero que nada analizando la funcion main nos podemos dar cuenta que nuestro input se almacena en un memset 
y se almacena en local_118 luego haces el xor con los bytes del key y por ende nuetro objetivo es el xor entre la key y la flag

que gracias al 

objdump -d sw-06 

lo que logramos 

es conseguir que el key empieza en 0x2018

analizando de vuelta el main la key tiene tamaño 14 o 0xe

print(hex(0x2018+0xe)) => 0x2026  OJO QUE EL STOP ADDRESS ES EXCLUSIVE, pero aca como sumamos 14 bytes en terminos de posicion
sucede que nos queda el start que digamos es 0, hasta el 13, que da justo 14 bytes y realmente no hay problema pero ibamos a tener basura al final y por mas que nos de 
la flag nuesro script de todas maneras es buena practica siempre tomar solo lo justo

y sabiendo esto tenemos el start address y el stop address

haciendo el comando

objdump -s -j .rodata --start-address=0x2018 --stop-address=0x2026 sw-06

obtenemos

b230bddc107ae17b2c3be2ec9901 que es el hex de la key

despues sabemos gracias al objdump que la key empieza en 0x2008 y nada haciendo la misma formulita

print(hex(0x2008+0xe)) => 0x2016

obtenemos gracias al comando

objdump -s -j .rodata --start-address=0x2008 --stop-address=0x2016 sw-06

lo siguiente 

2008d45cdcbb6b1ed34a4a5ed2dfac7c

que es le hex de la flag! 

ahora considero que queda hacer el xor de las dos 
