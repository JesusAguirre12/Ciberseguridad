leer la guia de bash para entender la siguiente solucion 

while true; do
  for file in *.zip; do
    # Verifica que el archivo exista por si no hay coincidencias de *.zip
    if [ -f "$file" ]; then
      unzip -q "$file" && rm "$file" && echo "Procesado y eliminado: $file"
    fi
  done
done


