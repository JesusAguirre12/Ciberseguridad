primero que nada hacemos 

objdump -h sw-03

y esto imprime todas las cabeceras en donde 

.super-secret-section 

destaca por sobre las demas asi que la leemos con

readelf -x .super-secret-section sw-03 

esto dice imprimeme el volcado de lo que sea que tenga la siguiente seccion, se indica con el indice o con el nombre

con objdump tambien se puede hacer

objdump -s -j .super-secret-section sw-03

-s dice que imprimas todo de 

-j todo de la siguiente seccion que se llama asi

flag{d03lvn4i}
